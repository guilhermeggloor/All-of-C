#include <stdio.h>

int main()
{
    //  array = a data structure that can store many values of the same data type.

    double price[10] = {5.0, 10.0, 15.0, 20.0, 25.0};

    printf("$%.2lf", price[4]);

    return 0;
}